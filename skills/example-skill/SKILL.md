---
name: example-skill
description: >-
  Minimal example skill showing repo layout. Safe to delete once you add real skills under skills/.
disable-model-invocation: true
metadata:
  version: "0.1.0"
---

# Example skill

Use this as a reference layout only:

- Keep `name` aligned with the folder name (`example-skill`).
- Prefer concise operational steps agents can follow verbatim.

## Workflow

1. Clarify the task with the user when ambiguous.
2. Apply the conventions documented in your team / repo.
